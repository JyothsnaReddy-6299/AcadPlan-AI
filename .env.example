# Backend API base URL (FastAPI runs on http://localhost:8000 by default)
VITE_API_URL=http://localhost:8000

# Set to "false" to talk to the live FastAPI backend instead of the in-browser mock API.
VITE_USE_MOCK_API=false
